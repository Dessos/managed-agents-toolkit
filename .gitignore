# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
*.egg
dist/
build/
.eggs/

# Virtual envs
.venv/
venv/
env/

# Tests + coverage
.pytest_cache/
.coverage
htmlcov/
.tox/
.mypy_cache/
.ruff_cache/

# IDE
.vscode/
.idea/
*.swp
*.swo

# Claude Code per-clone state (sessions, todos, local-only settings).
# NOTE: .claude/settings.json IS committed — it's the project-shared hook +
# permissions config. Same for .claude/{agents,commands,skills,hooks}/ —
# those are shared infra. Only operator-private state below is ignored.
.claude/settings.local.json
.claude/sessions/
.claude/todos/
.claude/projects/
.claude/statsig/
.claude/shell-snapshots/
.claude/ide/
.claude/state/
.claude/cache/

# Obsidian per-machine workspace state inside the vault.
docs/vault/.obsidian/

# Secrets
.env
.env.local
*.pem
*.key

# CMA runtime state
.cma-state.db
*.jsonl
cma-state.db

# OS
.DS_Store
Thumbs.db

# Project-local outputs that shouldn't be checked in
outputs/
deliverables/
.managed-agents/outputs/
.managed-agents/.state/
