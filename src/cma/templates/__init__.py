"""Template files shipped with the toolkit.

Loaded via :func:`importlib.resources.files` in :mod:`cma.cli.project`.
The actual contents are YAML files; this package only exists so that
``importlib.resources.files('cma.templates')`` resolves consistently
across editable, wheel, and zipapp installs.
"""

from __future__ import annotations
